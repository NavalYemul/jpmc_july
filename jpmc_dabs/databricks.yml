# This is a Databricks asset bundle definition for jpmc_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: jpmc_dabs
  uuid: fd7b25df-f917-4c6b-a509-bc6d209a26b1

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-1457738f-c3b9.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-1457738f-c3b9.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/priyankanyemul@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/priyankanyemul@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: priyankanyemul@gmail.com
        level: CAN_MANAGE
